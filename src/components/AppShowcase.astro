---
---

<section class="app-showcase">
  <div class="container">
    <div class="showcase-header" data-animate="fade-up">
      <h2 class="section-title">What Makes AirTALK Different</h2>
      <p class="section-subtitle">
        Think Omegle, but voice only and built with features that make chatting better. Connect with strangers who share your hobbies, meet people from around the world, stay safe with AI moderation, and chat without the awkwardness of video calls. Here's what sets AirTALK apart:
      </p>
    </div>

    <!-- Feature 1: Interests -->
    <div class="showcase-item" data-animate="fade-right">
      <div class="showcase-content">
        <span class="feature-label">Shared Interests</span>
        <h3 class="showcase-title">
          Talk to Strangers Who Share Your <span class="highlight">Interests</span>
        </h3>
        <p class="showcase-description">
          Select your interests and get matched with strangers who share them. AirTalk lets you connect over common passions instead of awkward small talk!
        </p>
      </div>
      <div class="showcase-image">
        <div class="mockup-wrapper">
          <img src="/mockups/filter_interest.webp" alt="Select your hobbies and get matched with strangers" loading="lazy" />
          <div class="floating-element star-1">⭐</div>
          <div class="floating-element emoji-1">🎮</div>
          <div class="floating-element emoji-2">☀️</div>
        </div>
      </div>
    </div>

    <!-- Feature 2: Safe Picture Sharing -->
    <div class="showcase-item reverse" data-animate="fade-left">
      <div class="showcase-content">
        <span class="feature-label">Safe Picture Sharing</span>
        <h3 class="showcase-title">
          Share Pictures Safely in Every <span class="highlight">Chat</span>
        </h3>
        <p class="showcase-description">
          Send text and share images while voice chatting. AI moderation blocks inappropriate content to keep conversations safe.
        </p>
      </div>
      <div class="showcase-image">
        <div class="mockup-wrapper">
          <img src="/mockups/chat.webp" alt="Chat interface with safe picture sharing" loading="lazy" />
          <div class="floating-element fire">🔥</div>
          <div class="floating-element smile">😊</div>
          <div class="floating-element plane">✈️</div>
        </div>
      </div>
    </div>

    <!-- Feature 3: International Friends -->
    <div class="showcase-item" data-animate="fade-right">
      <div class="showcase-content">
        <span class="feature-label">Global Connections</span>
        <h3 class="showcase-title">
          Make International <span class="highlight">Friends</span>
        </h3>
        <p class="showcase-description">
          Select the specific country or region you want to connect with. Our platform lets you explore new cultures and practice your language skills!
        </p>
      </div>
      <div class="showcase-image">
        <div class="mockup-wrapper">
          <img src="/mockups/filter_country.webp" alt="Filter by country to make international friends" loading="lazy" />
          <div class="floating-element globe">🌍</div>
          <div class="floating-element flag">🎌</div>
        </div>
      </div>
    </div>

    <!-- Feature 4: Best Match -->
    <div class="showcase-item reverse" data-animate="fade-left">
      <div class="showcase-content">
        <span class="feature-label">Premium Matching</span>
        <h3 class="showcase-title">
          Find Your Best <span class="highlight">Match</span> to Connect With
        </h3>
        <p class="showcase-description">
          Available for premium subscribers, our super-accurate AI analyzes voice patterns and matches you with your preferred gender.
        </p>
      </div>
      <div class="showcase-image">
        <div class="mockup-wrapper">
          <img src="/mockups/filter_gender.webp" alt="AI-powered matching for preferred gender" loading="lazy" />
          <div class="floating-element heart">💜</div>
          <div class="floating-element sparkle">✨</div>
        </div>
      </div>
    </div>

    <!-- Feature 5: Call History -->
    <div class="showcase-item" data-animate="fade-right">
      <div class="showcase-content">
        <span class="feature-label">Never Lose a Connection</span>
        <h3 class="showcase-title">
          Never Lose a <span class="highlight">Conversation</span>
        </h3>
        <p class="showcase-description">
          Access your call history anytime. If you get disconnected, you can easily reconnect and pick up where you left off.
        </p>
      </div>
      <div class="showcase-image">
        <div class="mockup-wrapper">
          <img src="/mockups/call_history.png" alt="Call history showing recent conversations for reconnection" loading="lazy" />
          <div class="floating-element clock">⏰</div>
          <div class="floating-element phone">📱</div>
        </div>
      </div>
    </div>

    <!-- Feature 6: Games -->
    <div class="showcase-item reverse" data-animate="fade-left">
      <div class="showcase-content">
        <span class="feature-label">Break the Ice</span>
        <h3 class="showcase-title">
          Play <span class="highlight">Games</span> While You Chat
        </h3>
        <p class="showcase-description">
          You can play tic-tac-toe with your match to add a quick burst of fun. It helps break the ice and keeps the chat flowing.
        </p>
      </div>
      <div class="showcase-image">
        <div class="mockup-wrapper">
          <img src="/mockups/games.webp" alt="In-app games like tic-tac-toe" loading="lazy" />
          <div class="floating-element game">🎮</div>
          <div class="floating-element trophy">🏆</div>
          <div class="floating-element dice">🎲</div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .app-showcase {
    padding: 6rem 0;
    position: relative;
    overflow: hidden;
    background: 
      radial-gradient(ellipse at 20% 30%, rgba(79, 127, 255, 0.08) 0%, transparent 50%),
      radial-gradient(ellipse at 80% 70%, rgba(139, 127, 255, 0.08) 0%, transparent 50%);
  }

  .app-showcase::before {
    content: '';
    position: absolute;
    top: 10%;
    right: -10%;
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, rgba(79, 127, 255, 0.1) 0%, transparent 70%);
    border-radius: 50%;
    filter: blur(60px);
    animation: float-bubble 12s ease-in-out infinite;
    will-change: transform;
    transform: translateZ(0);
    backface-visibility: hidden;
  }

  .app-showcase::after {
    content: '';
    position: absolute;
    bottom: 20%;
    left: -10%;
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, rgba(139, 127, 255, 0.1) 0%, transparent 70%);
    border-radius: 50%;
    filter: blur(50px);
    animation: float-bubble 10s ease-in-out infinite reverse;
    will-change: transform;
    transform: translateZ(0);
    backface-visibility: hidden;
  }

  @keyframes float-bubble {
    0%, 100% {
      transform: translate3d(0, 0, 0) scale(1);
    }
    33% {
      transform: translate3d(-20px, -30px, 0) scale(1.1);
    }
    66% {
      transform: translate3d(20px, 20px, 0) scale(0.9);
    }
  }

  .showcase-header {
    text-align: center;
    margin-bottom: 5rem;
    position: relative;
    z-index: 1;
  }

  .showcase-badge {
    display: inline-block;
    padding: 0.5rem 1.5rem;
    background: rgba(61, 61, 133, 0.5);
    border: 1px solid var(--border);
    border-radius: 50px;
    font-size: 0.9rem;
    color: var(--text-muted);
    font-weight: 500;
    margin-bottom: 1.5rem;
  }

  .showcase-item {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
    margin-bottom: 6rem;
    position: relative;
    z-index: 1;
  }

  .showcase-item:last-child {
    margin-bottom: 0;
  }

  .showcase-item.reverse {
    direction: rtl;
  }

  .showcase-item.reverse > * {
    direction: ltr;
  }

  .showcase-content {
    max-width: 500px;
  }

  .feature-label {
    display: block;
    font-size: 0.95rem;
    color: var(--text-dim);
    font-style: italic;
    margin-bottom: 0.75rem;
    opacity: 0.9;
  }

  .showcase-title {
    font-size: clamp(1.75rem, 3vw, 2.5rem);
    font-weight: 700;
    margin-bottom: 1.25rem;
    line-height: 1.2;
    color: var(--text);
  }

  .showcase-title .highlight {
    background: linear-gradient(135deg, var(--primary-light) 0%, var(--accent-light) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .showcase-description {
    font-size: 1.05rem;
    color: var(--text-muted);
    line-height: 1.8;
  }

  .showcase-image {
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .mockup-wrapper {
    position: relative;
    max-width: 400px;
    width: 100%;
  }

  .mockup-wrapper img {
    width: 100%;
    height: auto;
    transition: transform 0.4s ease;
  }

  .showcase-item:hover .mockup-wrapper img {
    transform: translateY(-8px) scale(1.02);
  }

  /* Floating decorative elements */
  .floating-element {
    position: absolute;
    font-size: 2rem;
    animation: float 3s ease-in-out infinite;
    filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.3));
    z-index: 10;
    will-change: transform;
    transform: translateZ(0);
    backface-visibility: hidden;
  }

  .star-1 {
    top: -10%;
    right: 5%;
    animation-delay: 0s;
  }

  .emoji-1 {
    top: 20%;
    left: -10%;
    animation-delay: 0.5s;
    font-size: 1.5rem;
  }

  .emoji-2 {
    top: 5%;
    left: 10%;
    animation-delay: 1s;
    font-size: 1.75rem;
  }

  .fire {
    top: -5%;
    left: 5%;
    animation-delay: 0.3s;
  }

  .smile {
    top: 15%;
    right: -5%;
    animation-delay: 0.7s;
  }

  .plane {
    bottom: 10%;
    left: -8%;
    animation-delay: 0.2s;
    font-size: 1.5rem;
  }

  .globe {
    top: -8%;
    right: 10%;
    animation-delay: 0.4s;
  }

  .flag {
    bottom: 5%;
    left: -5%;
    animation-delay: 0.8s;
    font-size: 1.5rem;
  }

  .heart {
    top: -10%;
    left: 15%;
    animation-delay: 0.2s;
  }

  .sparkle {
    bottom: 15%;
    right: -8%;
    animation-delay: 0.6s;
  }

  .clock {
    top: -5%;
    right: 15%;
    animation-delay: 0.3s;
  }

  .phone {
    bottom: 10%;
    left: -10%;
    animation-delay: 0.7s;
  }

  .game {
    top: -8%;
    left: 10%;
    animation-delay: 0.1s;
  }

  .trophy {
    top: 15%;
    right: -8%;
    animation-delay: 0.5s;
  }

  .dice {
    bottom: 5%;
    left: -5%;
    animation-delay: 0.9s;
    font-size: 1.5rem;
  }

  @keyframes float {
    0%, 100% {
      transform: translate3d(0, 0, 0) rotate(0deg);
    }
    50% {
      transform: translate3d(0, -12px, 0) rotate(5deg);
    }
  }

  /* Responsive Design */
  @media (max-width: 1024px) {
    .showcase-item {
      gap: 3rem;
    }

    .mockup-wrapper {
      max-width: 350px;
    }
  }

  @media (max-width: 768px) {
    .app-showcase {
      padding: 4rem 0;
    }

    .showcase-header {
      margin-bottom: 3rem;
    }

    .showcase-item {
      grid-template-columns: 1fr;
      gap: 2rem;
      margin-bottom: 4rem;
      text-align: center;
    }

    .showcase-item.reverse {
      direction: ltr;
    }

    .showcase-content {
      max-width: 100%;
      order: 1;
    }

    .showcase-image {
      order: 2;
    }

    .mockup-wrapper {
      max-width: 300px;
    }

    .floating-element {
      font-size: 1.5rem;
    }

    .showcase-title {
      font-size: 1.5rem;
    }

    .showcase-description {
      font-size: 0.95rem;
    }
  }

  @media (max-width: 480px) {
    .showcase-item {
      gap: 1.5rem;
      margin-bottom: 3rem;
    }

    .mockup-wrapper {
      max-width: 260px;
    }

    .floating-element {
      font-size: 1.25rem;
    }

    .showcase-badge {
      font-size: 0.8rem;
      padding: 0.4rem 1rem;
    }

    .feature-label {
      font-size: 0.85rem;
    }

    .showcase-title {
      font-size: 1.3rem;
    }

    .showcase-description {
      font-size: 0.9rem;
      line-height: 1.6;
    }
  }
</style>

